//go:build tools

// Удерживает gomobile в зависимостях модуля.
//
// Собственный код gobind генерирует с обращениями к пакету bind этого самого
// gomobile, и если его нет в go.mod, сборка привязок падает на неразрешённом
// импорте. Тег tools нужен, чтобы файл не попадал в обычную сборку — он здесь
// только ради go.mod, и go mod tidy без него выбрасывал зависимость как
// неиспользуемую.
//
// Форк SagerNet, а не оригинал: sing-box собирается только им.
package zyngcore

import _ "github.com/sagernet/gomobile/bind"
