# Генерируется из project.yml командой xcodegen — в репозитории не нужен.
Zyng.xcodeproj/
Zyng.xcworkspace/

build/
DerivedData/
.DS_Store
xcuserdata/
*.xcuserstate

# Ядро sing-box: собирается ./build-libbox.sh, в git не хранится (сотни МБ).
Frameworks/
.build/
